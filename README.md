# Developing

```shell
nvm install 10
npm i
pip install -r requirements-dev.txt
./watch
# In a separate shell
./manage.py runserver
# In yed another separate shell
npm start
```

# Running tests

```shell
detox`
npm run test
```
